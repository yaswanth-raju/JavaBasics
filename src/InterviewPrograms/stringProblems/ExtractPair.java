package InterviewPrograms.stringProblems;

import java.util.AbstractMap;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.stream.IntStream;

public class ExtractPair {
    public static void main(String[] args) {
        String str="a 1 b 3 c 4 d 5";
//        IntStream.range(0,str.length())
//                .mapToObj(index ->str.charAt(index))
//                .filter(chr->chr!=' ')
////                        .reduce((a,b)->new AbstractMap.SimpleEntry<Character,Character>(a,b))
//        Arrays.stream(str.split(" ")).filter(i->i%2==0).reduce((a,b)->AbstractEn);
    }
}
